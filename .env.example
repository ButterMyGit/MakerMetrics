# Firebase — download from Firebase Console → Project Settings → Service Accounts → Generate new private key
FIREBASE_CREDENTIALS=secrets/firebase.json

# Directory that watcher monitors for incoming Etsy CSV exports
WATCH_DIR=data/watch

# Seconds to wait for a matching SoldOrders file after a SoldOrderItems file arrives
PAIR_WAIT_SECONDS=30
